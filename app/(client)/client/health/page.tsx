import { ClientStubPage } from '@/components/client/client-stub-page';

export default function ClientHealthPage() {
    return (
        <ClientStubPage
            title="Health Sync"
            panelTitle="Coming soon"
            description="Wearable connections and synced metrics will appear here once Health Sync is wired for members."
        />
    );
}
