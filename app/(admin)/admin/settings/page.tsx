import { AdminStubPage } from "@/components/admin/admin-stub-page";

export default function SettingsPage() {
  return (
    <AdminStubPage
      title="Settings"
      panelTitle="Gym org profile"
      description="Create or update your GymOrg (name, timezone Asia/Kolkata default). Next slice after login."
    />
  );
}
