import { AdminStubPage } from "@/components/admin/admin-stub-page";

export default function AttendancePage() {
  return (
    <AdminStubPage
      title="Attendance"
      panelTitle="Desk attendance"
      description="Mark attendance and review logs. Entitlement follows subscription dates, not payment status."
    />
  );
}
