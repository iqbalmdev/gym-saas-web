import { AdminStubPage } from "@/components/admin/admin-stub-page";

export default function MembersPage() {
  return (
    <AdminStubPage
      title="Members"
      panelTitle="Roster"
      description="Invites, offboard, and block check-in — gym-owned data in a dense table view."
    />
  );
}
