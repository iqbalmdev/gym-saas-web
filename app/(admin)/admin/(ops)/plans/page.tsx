import { AdminStubPage } from "@/components/admin/admin-stub-page";

export default function PlansPage() {
  return (
    <AdminStubPage
      title="Plans"
      panelTitle="Plan catalog"
      description="Catalog and assign subscription lines with payment badges — still no auto lock on unpaid."
    />
  );
}
