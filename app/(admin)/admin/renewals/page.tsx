import { AdminStubPage } from "@/components/admin/admin-stub-page";

export default function RenewalsPage() {
  return (
    <AdminStubPage
      title="Renewals"
      panelTitle="Renewals inbox"
      description="Members due within T-2 and unpaid nudges will land here as a calm ops table — not auto lock-outs."
    />
  );
}
