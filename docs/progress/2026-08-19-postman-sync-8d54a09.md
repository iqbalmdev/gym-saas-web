# 2026-08-19 — Pulled Postman collection tip `8d54a09`

Sibling `gym-backend-postman` fast-forwarded `e01816b..8d54a09`. New folders: Nutrition, Coaching, Health Sync. Roster gained List Gym Trainers. OTP / staff-invite contracts unchanged — auth guides not edited.

Postman cloud inject did not run: MCP returned `401 Invalid API Key` after `mcp_auth`. Prod `GET /health` still `200` ok. Re-login Postman MCP, then re-run **sync-postman-collection**. Until then, Desktop Import from sibling JSON or the `8d54a09` raw URLs in `docs/postman-sync.md`.
