---
description: Sibling gym-backend-postman + Postman cloud — no vendored collection JSON
alwaysApply: true
---

# Postman sync

- **Publish:** sibling `../gym-backend-postman` (`gym-saas.code-workspace`).
- **Working copy:** Postman cloud — `docs/postman-sync.md`.
- **Never** store `postman/*.json` in this repo.
- Sync: skill **sync-postman-collection** → sibling `git pull` (or GitHub) → Postman MCP inject → delete temp prep.
- Auth/invites: `docs/api/client-auth.md`, `docs/api/staff-invites.md`.
- Prod: `https://gym-backend-lovat-mu.vercel.app` · Local: `http://localhost:3000`.
- Never invent endpoints; prefer Postman Examples after inject.
