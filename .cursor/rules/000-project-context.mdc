---
description: "Gym SaaS — project context, always loaded"
alwaysApply: true
---

# Project: Gym SaaS (India-first, MVP)

Multi-surface product, one backend:
- S1 Mobile (React Native) — Client + Trainer core flows, light Admin
- S2 Web Admin (Next.js, App Router) — THIS REPO. Dashboards, renewals inbox,
  CRM pipeline, roster tables, desk attendance, plan catalog
- S3 API (Node/Express + Supabase/Postgres) — auth (email OTP), tenancy,
  jobs, push, food catalog, audit trail

**This repo owns no domain data.** Business rules (membership ACTIVE,
subscription dates, DataGrants, block check-in) are enforced in S3. The web
app displays API state and posts user intent — it never re-derives
entitlement in JSX and never talks to Postgres.

## Non-negotiable domain rules
- Two account lanes: CLIENT and STAFF. A CLIENT can never become STAFF and
  vice versa. Never write code that lets a user switch lanes.
- Every gym is a tenant (GymOrg). Every gym-scoped call is made against the
  active `gym_org_id` from the session's affiliation — never a value sent
  from the client. There is no cross-tenant read, ever.
- Joins are invite-only. There are no open join codes. Do not build or
  suggest an open-code join flow (plan §13 non-goals).
- Personal fitness/health data (progress, nutrition, wearables) is
  CLIENT-OWNED. Trainers/Admins can only read it if a DataGrant exists for
  that specific scope (PROGRESS, CALORIES, WEARABLES). No grant = no access,
  no exceptions, no "just for debugging."
- Roles: `CLIENT`, `STAFF_UNASSIGNED`, `TRAINER`, `ADMIN`. Frozen system
  codes — gyms cannot edit roles or permissions. A role code alone never
  unlocks data (see `security-data-access.mdc`).
- Billing is base + addons with price snapshots — never recompute historical
  lines from current prices; render the snapshot the API returns.
- Entitlement follows **subscription dates**, not `payment_status`. Unpaid
  shows a badge; it never locks check-in. Check-in lock is a manual Admin
  action only.
- English UI only for this MVP. Do not add i18n scaffolding.

## Source of truth
Domain: `docs/PRD.md`, `docs/product-flows.md`, `docs/CONTEXT.md` (glossary).
Build contract: `docs/architecture-plan.md`. Live stage: `docs/PROGRESS.md`.
API contract: sibling `gym-backend-postman` — never invent endpoints.

If a request conflicts with these, flag the conflict instead of silently
picking one.
